#ifndef NAGARROREMOTELEARNING_ENUMS_H
#define NAGARROREMOTELEARNING_ENUMS_H

enum MeasureUnit {
    CELSIUS,
    PERCENT,
};

enum RoomType {
    LIVING_ROOM,
    BEDROOM,
    KITCHEN,
};

enum DeviceType {
    LIGHT,
    HEATER,
};

#endif
