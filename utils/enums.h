#ifndef NAGARROREMOTELEARNING_ENUMS_H
#define NAGARROREMOTELEARNING_ENUMS_H

enum MeasureUnit {
    CELSIUS,
    PERCENT,
    UNKNOWN,
};

enum RoomType {
    LIVING_ROOM,
    BEDROOM,
    KITCHEN,
};

enum DeviceType {
    LIGHT,
    HEATER,
    AIR_CONDITIONER,
};

#endif
